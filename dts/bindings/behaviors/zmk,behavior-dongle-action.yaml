description: Dongle Action Behavior — dispatches Pacman display/game actions

compatible: "zmk,behavior-dongle-action"

include: base.yaml

properties:
  label:
    type: string
    required: true
  "#binding-cells":
    type: int
    required: true
    const: 1
