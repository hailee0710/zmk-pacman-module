/*
 * nice!nano v2 board-specific overrides
 *
 * P0.17 is the onboard charge LED on nice!nano v2.
 * Move display RESET to P0.05 (D3) to avoid conflict.
 *
 * To use a different pin, override reset-gpios in your own overlay.
 */
&st7789p3 {
    reset-gpios = <&gpio0 5 GPIO_ACTIVE_LOW>;
};
